# -*- coding: utf-8 -*-
"""Internal-coordinate (IC) reference data: grow-path topology and ideal
per-residue templates.

* :mod:`.protein` -- protein topology (``IC_PATH`` side-chain grow quads,
  ``BACKBONE_IC_PATH`` uniform backbone grow quads, ``MAINCHAIN_ATOMS``)
  and the per-residue x secondary-structure x rotamer
  ``InternalCoord`` template store (:mod:`.protein.template`).
"""