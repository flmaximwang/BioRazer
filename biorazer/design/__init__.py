"""
This package adopts design entries (Entry) and design libraries (Library) as the core data structures
for organizing protein design results.

The objects themselves -- the Entry hierarchy, Library, SingleTest and their sequence-specialised
counterparts -- are enumerated in biorazer.design.objects, which is where they are imported from.
"""
